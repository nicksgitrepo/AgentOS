# Preserved platform handoff: 02-bootstrap-and-context


## Platform gate handoff addendum

The platform-foundation handoff is now recorded in
`docs/rapid-foundations/02-bootstrap-and-context-platform-handoff.md`. It is
source-bound to the same committed head/tree, preserves the dirty-worktree
and private-identity boundaries, carries the shared skeleton, directory and
custody rules, technology recommendations, routing/feature gates, shared
contracts, UI/design direction, security constraints, and unresolved owner
questions, and explicitly blocks feature-lane release until the Controller has
independently audited and merged one clean platform tree.

