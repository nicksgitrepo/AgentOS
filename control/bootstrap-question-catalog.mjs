export const BOOTSTRAP_QUESTIONS = Object.freeze([
  Object.freeze({
    id: "intent.audience",
    kind: "TEXT",
    prompt: "Who is this for?",
    required: true,
  }),
  Object.freeze({
    id: "intent.outcome",
    kind: "TEXT",
    prompt: "What should it make easier?",
    required: true,
  }),
  Object.freeze({
    id: "intent.first_result",
    kind: "TEXT",
    prompt: "What would a useful first result look like?",
    required: true,
  }),
  Object.freeze({
    id: "project.starting_point",
    kind: "CHOICE",
    prompt: "Are we starting fresh or changing something that exists?",
    choices: Object.freeze([
      Object.freeze({value: "NEW", label: "Start something new"}),
      Object.freeze({value: "EXTEND", label: "Add to something that exists"}),
      Object.freeze({value: "REPAIR", label: "Repair something that exists"}),
      Object.freeze({value: "REPLACE", label: "Replace something that exists"}),
    ]),
    required: true,
  }),
  Object.freeze({
    id: "development.workflow",
    kind: "CHOICE",
    prompt: "How would you like the team to build and check the project?",
    choices: Object.freeze([
      Object.freeze({value: "PYRAMID", label: "Build and check in layers"}),
      Object.freeze({value: "COLLABORATIVE_AUDIT", label: "Use six focused checkers at a time"}),
      Object.freeze({value: "RECOMMEND", label: "Recommend the best fit after discovery"}),
    ]),
    required: true,
  }),
  Object.freeze({
    id: "scope.allowed",
    kind: "TEXT",
    prompt: "What should this work touch?",
    required: true,
  }),
  Object.freeze({
    id: "scope.non_goals",
    kind: "TEXT",
    prompt: "What should I explicitly leave out for now?",
    required: true,
  }),
  Object.freeze({
    id: "existing.invariants",
    kind: "TEXT",
    prompt: "What existing behavior or information must remain unchanged?",
    required: true,
    when: Object.freeze([{question_id: "project.starting_point", operator: "NOT_EQUALS", value: "NEW"}]),
  }),
  Object.freeze({
    id: "project.capabilities",
    kind: "MULTI_CHOICE",
    prompt: "Which parts will the first useful version need?",
    choices: Object.freeze([
      Object.freeze({value: "USER_INTERFACE", label: "Screens or user interactions"}),
      Object.freeze({value: "BACKEND_API", label: "Backend services, APIs, or workers"}),
      Object.freeze({value: "DATA", label: "Saved or imported information"}),
      Object.freeze({value: "ACCESS", label: "Accounts, roles, or permissions"}),
      Object.freeze({value: "AI_SEARCH", label: "AI, search, or recommendations"}),
      Object.freeze({value: "INTEGRATIONS", label: "Outside services or APIs"}),
      Object.freeze({value: "HARDWARE_REALTIME", label: "Devices, sensors, or live signals"}),
      Object.freeze({value: "COMMERCE", label: "Payments, billing, or money movement"}),
      Object.freeze({value: "SAFETY_REGULATED", label: "Safety-critical or regulated work"}),
      Object.freeze({value: "NONE", label: "None of these yet"}),
    ]),
    required: true,
  }),
  Object.freeze({
    id: "backend.behavior",
    kind: "TEXT",
    prompt: "What services, APIs, or background work are needed?",
    required: true,
    when: Object.freeze([{question_id: "project.capabilities", operator: "CONTAINS", value: "BACKEND_API"}]),
  }),
  Object.freeze({
    id: "experience.channels",
    kind: "MULTI_CHOICE",
    prompt: "Where will people use the first version?",
    choices: Object.freeze([
      Object.freeze({value: "WEB", label: "Web browser"}),
      Object.freeze({value: "MOBILE", label: "Phone or tablet"}),
      Object.freeze({value: "DESKTOP", label: "Desktop app"}),
      Object.freeze({value: "CLI", label: "Command line"}),
      Object.freeze({value: "EMBEDDED", label: "Device or embedded screen"}),
    ]),
    required: true,
    when: Object.freeze([{question_id: "project.capabilities", operator: "CONTAINS", value: "USER_INTERFACE"}]),
  }),
  Object.freeze({
    id: "data.posture",
    kind: "TEXT",
    prompt: "What information is saved, and how sensitive is it?",
    required: true,
    when: Object.freeze([{question_id: "project.capabilities", operator: "CONTAINS", value: "DATA"}]),
  }),
  Object.freeze({
    id: "data.lifecycle",
    kind: "TEXT",
    prompt: "Where does information come from, change, and leave?",
    required: true,
    when: Object.freeze([{question_id: "project.capabilities", operator: "CONTAINS", value: "DATA"}]),
  }),
  Object.freeze({
    id: "access.model",
    kind: "TEXT",
    prompt: "Who can do what, and who must stay separated?",
    required: true,
    when: Object.freeze([{question_id: "project.capabilities", operator: "CONTAINS", value: "ACCESS"}]),
  }),
  Object.freeze({
    id: "ai.behavior",
    kind: "TEXT",
    prompt: "What should the AI or search do for people?",
    required: true,
    when: Object.freeze([{question_id: "project.capabilities", operator: "CONTAINS", value: "AI_SEARCH"}]),
  }),
  Object.freeze({
    id: "ai.truth_boundary",
    kind: "TEXT",
    prompt: "What must it prove, cite, or refuse to guess?",
    required: true,
    when: Object.freeze([{question_id: "project.capabilities", operator: "CONTAINS", value: "AI_SEARCH"}]),
  }),
  Object.freeze({
    id: "integrations.boundaries",
    kind: "TEXT",
    prompt: "What may outside services read, change, or trigger?",
    required: true,
    when: Object.freeze([{question_id: "project.capabilities", operator: "CONTAINS", value: "INTEGRATIONS"}]),
  }),
  Object.freeze({
    id: "hardware.constraints",
    kind: "TEXT",
    prompt: "What physical limits or live timing must be respected?",
    required: true,
    when: Object.freeze([{question_id: "project.capabilities", operator: "CONTAINS", value: "HARDWARE_REALTIME"}]),
  }),
  Object.freeze({
    id: "commerce.boundaries",
    kind: "TEXT",
    prompt: "What money actions require confirmation or reconciliation?",
    required: true,
    when: Object.freeze([{question_id: "project.capabilities", operator: "CONTAINS", value: "COMMERCE"}]),
  }),
  Object.freeze({
    id: "risk.applicability",
    kind: "TEXT",
    prompt: "Which safety rules, laws, or standards apply here?",
    required: true,
    when: Object.freeze([{question_id: "project.capabilities", operator: "CONTAINS", value: "SAFETY_REGULATED"}]),
  }),
  Object.freeze({
    id: "workflow.steps",
    kind: "TEXT",
    prompt: "What steps should the first version follow?",
    required: true,
  }),
  Object.freeze({
    id: "technology.constraints",
    kind: "TEXT",
    prompt: "What technology should we use, keep, or avoid?",
    required: true,
  }),
  Object.freeze({
    id: "operations.conditions",
    kind: "TEXT",
    prompt: "What speed, scale, offline, or availability limits matter?",
    required: true,
  }),
  Object.freeze({
    id: "quality.priorities",
    kind: "MULTI_CHOICE",
    prompt: "Which qualities matter most for the first version?",
    choices: Object.freeze([
      Object.freeze({value: "CORRECTNESS", label: "Correctness"}),
      Object.freeze({value: "SECURITY", label: "Security and privacy"}),
      Object.freeze({value: "RELIABILITY", label: "Reliability and recovery"}),
      Object.freeze({value: "USABILITY", label: "Usability and accessibility"}),
      Object.freeze({value: "PERFORMANCE", label: "Speed and efficiency"}),
      Object.freeze({value: "PORTABILITY", label: "Portability and compatibility"}),
    ]),
    required: true,
  }),
  Object.freeze({
    id: "acceptance.conditions",
    kind: "TEXT",
    prompt: "How will you know the result is good enough?",
    required: true,
  }),
  Object.freeze({
    id: "boundaries.hard",
    kind: "TEXT",
    prompt: "What must always make me stop?",
    required: true,
  }),
  Object.freeze({
    id: "boundaries.soft",
    kind: "TEXT",
    prompt: "What should make me pause and check with you?",
    required: true,
  }),
  Object.freeze({
    id: "governance.memory",
    kind: "BOOLEAN",
    prompt: "Should I remember this next time?",
    choices: Object.freeze([
      Object.freeze({value: true, label: "Yes"}),
      Object.freeze({value: false, label: "No"}),
    ]),
    required: true,
  }),
  Object.freeze({
    id: "delivery.finish",
    kind: "CHOICE",
    prompt: "When the first version is ready, what should happen?",
    choices: Object.freeze([
      Object.freeze({value: "REVIEW", label: "Leave it ready for review"}),
      Object.freeze({value: "SAVE", label: "Save it safely for later"}),
      Object.freeze({value: "SHARE", label: "Share the saved work"}),
      Object.freeze({value: "LIVE", label: "Put it live"}),
    ]),
    required: true,
  }),
  Object.freeze({
    id: "terminology.preferred",
    kind: "TEXT",
    prompt: "Which words should we use consistently?",
    required: false,
  }),
  Object.freeze({
    id: "governance.providers",
    kind: "CHOICE",
    prompt: "Should outside services be used?",
    choices: Object.freeze([
      Object.freeze({value: "LOCAL_ONLY", label: "Use only this workspace"}),
      Object.freeze({value: "OWNER_APPROVAL", label: "Ask before using outside services"}),
      Object.freeze({value: "DEFERRED", label: "Decide later"}),
    ]),
    required: false,
    default_value: "LOCAL_ONLY",
  }),
  Object.freeze({
    id: "governance.retention",
    kind: "CHOICE",
    prompt: "How long should saved information be kept?",
    choices: Object.freeze([
      Object.freeze({value: "SESSION_ONLY", label: "This session only"}),
      Object.freeze({value: "PROJECT_LIFETIME", label: "For this project"}),
      Object.freeze({value: "OWNER_REVIEW", label: "Ask before keeping it"}),
    ]),
    required: false,
    default_value: "OWNER_REVIEW",
  }),
  Object.freeze({
    id: "delivery.intent",
    kind: "CHOICE",
    prompt: "Where should the result go first?",
    choices: Object.freeze([
      Object.freeze({value: "LOCAL_REVIEW", label: "Keep it here for review"}),
      Object.freeze({value: "SAFE_STORAGE", label: "Save it for later"}),
      Object.freeze({value: "OWNER_DECIDES", label: "Let me decide later"}),
    ]),
    required: false,
    default_value: "LOCAL_REVIEW",
  }),
  Object.freeze({
    id: "governance.review_interval",
    kind: "CHOICE",
    prompt: "How often should I pause to review progress?",
    choices: Object.freeze([
      Object.freeze({value: 15, label: "15 minutes (recommended)"}),
      Object.freeze({value: 30, label: "30 minutes"}),
      Object.freeze({value: 60, label: "60 minutes"}),
    ]),
    required: false,
    default_value: 15,
  }),
]);
