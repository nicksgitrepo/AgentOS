# Control

Executable campaign, Bootstrap Discovery, Bootstrap Interview, legacy
preservation, question-tree, evidence-library, and authority-maintenance
components. `guided-bootstrap` and `dynamic-bootstrap` remain compatibility
aliases for previously sealed 2.1rc state.
