#!/usr/bin/env node

/* Operational evaluator for the Data Router candidate. */
import fs from "node:fs";
import path from "node:path";
import os from "node:os";
import {createHash} from "node:crypto";
import {pathToFileURL} from "node:url";
import {canonicalDigest} from "./content-addressing.mjs";
import {evaluateDataRouterBoundary, DATA_ROUTER_BOUNDARY_SCHEMA} from "./data-router-boundary-gate.mjs";

export const DATA_ROUTER_PACKAGE_EVALUATION_SCHEMA = "agentos.specialist_data_router_package_operational_evaluation.v1";
const ROOT = path.resolve(path.dirname(new URL(import.meta.url).pathname), "..");
const PACKAGE_RELATIVE = "specialist-blocks/wave-02/data-router";
const BLOCK_ID = "specialist.data.router";
const sha = (bytes) => createHash("sha256").update(bytes).digest("hex");
const fail = (message, code = "DATA_ROUTER_PACKAGE_EVALUATION_INVALID") => { const e = new Error(message); e.code = code; throw e; };
const assert = (value, message, code) => { if (!value) fail(message, code); };
const read = (file) => { assert(fs.existsSync(file), `${file} is missing`, "DATA_ROUTER_PACKAGE_FILE_MISSING"); return fs.readFileSync(file); };
const readJson = (file) => JSON.parse(read(file));
const resultDigest = (value) => canonicalDigest({...value, result_sha256:null});
function files(root) { const out=["block.json","sources.lock","gates/manifest.json","evaluation.json","handoff.json"]; for(const name of fs.readdirSync(path.join(root,"gates")).filter(n=>n.endsWith(".gate"))) out.push(`gates/${name}`); for(const name of fs.readdirSync(path.join(root,"fixtures")).filter(n=>n.endsWith(".json"))) out.push(`fixtures/${name}`); return out.sort(); }
function fixtures(expectedClasses) {
  const root=path.join(ROOT,PACKAGE_RELATIVE,"fixtures"); const names=fs.readdirSync(root).filter(n=>n.endsWith(".json")).sort(); assert(names.length===expectedClasses.length && new Set(names).size===expectedClasses.length,"Data Router fixture inventory is not exact","DATA_ROUTER_FIXTURE_INVENTORY_INVALID"); const map=new Map();
  for(const name of names){const bytes=read(path.join(root,name));const fixture=JSON.parse(bytes);assert(fixture.block_id===BLOCK_ID&&typeof fixture.fixture_id==="string",`Data Router fixture ${name} identity is invalid`,`DATA_ROUTER_FIXTURE_ID_INVALID`);assert(fixture.vector?.entrypoint==="control/data-router-boundary-gate.mjs#evaluateDataRouterBoundary",`Data Router fixture ${name} is not operational`,`DATA_ROUTER_FIXTURE_UNBOUND`);assert(fixture.vector.input?.schema===DATA_ROUTER_BOUNDARY_SCHEMA,`Data Router fixture ${name} schema mismatch`,`DATA_ROUTER_FIXTURE_INPUT_INVALID`);assert(fixture.vector.expected_readback?.disposition&&fixture.vector.expected_readback?.route&&fixture.vector.expected_readback?.error_code,`Data Router fixture ${name} lacks expectation`,`DATA_ROUTER_FIXTURE_EXPECTATION_INVALID`);assert(!map.has(fixture.fixture_id),`Duplicate Data Router fixture ${name}`,`DATA_ROUTER_FIXTURE_ALIAS`);map.set(fixture.fixture_id,{fixture,file_sha256:sha(bytes)});}
  assert([...map.values()].map(v=>v.fixture.class).sort().join("\0")===expectedClasses.slice().sort().join("\0"),"Data Router fixture classes do not match package inventory","DATA_ROUTER_FIXTURE_CLASS_INVENTORY_INVALID"); return map;
}
async function mutation(fixture){const dir=fs.mkdtempSync(path.join(os.tmpdir(),"agentos-data-router-mutation-"));try{const control=path.join(dir,"control");fs.mkdirSync(control,{recursive:true});for(const dep of ["content-addressing.mjs","persisted-record-privacy.mjs"])fs.copyFileSync(path.join(ROOT,"control",dep),path.join(control,dep));const target=path.join(control,"data-router-boundary-gate.mjs");let source=fs.readFileSync(path.join(ROOT,"control/data-router-boundary-gate.mjs"),"utf8");const anchor='return result("DENY", "NO_ROUTER_SIDE_EFFECT", "DATA_ROUTER_OPERATION_FORBIDDEN", input);';assert(source.includes(anchor),"Data Router mutation anchor missing","DATA_ROUTER_MUTATION_ANCHOR_MISSING");source=source.replace(anchor,'return result("ROUTE", "SPECIALIST_HANDOFF", "MUTATED_OPERATION_ALLOWED", input, {routing_allowed:true, selected_specialist:"specialist.data.postgresql-rls"});');fs.writeFileSync(target,source,{flag:"wx"});const mod=await import(`${pathToFileURL(target).href}?mutation=${Date.now()}`);const observed=mod.evaluateDataRouterBoundary(fixture.vector.input);return {status:observed.disposition==="ROUTE"?"WEAKENED":"INTACT",mutation_detected:observed.disposition==="ROUTE",expected_disposition:"DENY",observed_disposition:observed.disposition,result_sha256:canonicalDigest(observed)};}finally{fs.rmSync(dir,{recursive:true,force:true});}}
export async function evaluateDataRouterPackage(){const packageRoot=path.join(ROOT,PACKAGE_RELATIVE);const block=readJson(path.join(packageRoot,"block.json"));assert(block.block_id===BLOCK_ID&&block.lifecycle==="CANDIDATE"&&block.activation==="OFF","Data Router package is not an inactive candidate","DATA_ROUTER_PACKAGE_STATE_INVALID");const packageFiles=files(packageRoot);const digests=packageFiles.map(relative_path=>({relative_path:`${PACKAGE_RELATIVE}/${relative_path}`,sha256:sha(read(path.join(packageRoot,relative_path)))}));assert(packageFiles.filter(p=>p.startsWith("gates/")&&p.endsWith(".gate")).length===12,"Data Router gate inventory is incomplete","DATA_ROUTER_GATE_INVENTORY_INVALID");const expectedClasses=block.evaluation?.fixture_classes??[];const map=fixtures(expectedClasses);const results=[];for(const info of [...map.values()].sort((a,b)=>a.fixture.fixture_id.localeCompare(b.fixture.fixture_id))){const f=info.fixture,expected=f.vector.expected_readback;let actual;try{actual=evaluateDataRouterBoundary(f.vector.input);}catch(e){fail(`${f.fixture_id} execution failed: ${e.code??e.message}`,"DATA_ROUTER_HOSTILE_EXECUTION_FAILED");}const zero=Object.values(actual.external_side_effects).every(v=>v===0);const checks=[{assertion:"TYPED_DISPOSITION",observed:actual.disposition===expected.disposition,evidence:`${actual.disposition}/${expected.disposition}`},{assertion:"TYPED_ROUTE",observed:actual.route===expected.route,evidence:`${actual.route}/${expected.route}`},{assertion:"TYPED_ERROR",observed:actual.error_code===expected.error_code,evidence:`${actual.error_code}/${expected.error_code}`},{assertion:"NO_DATA_SIDE_EFFECT",observed:zero,evidence:JSON.stringify(actual.external_side_effects)},{assertion:"ACCEPTANCE_FORBIDDEN",observed:actual.acceptance_allowed===false,evidence:`${actual.acceptance_allowed}`}];assert(checks.every(c=>c.observed),`${f.fixture_id} hostile result failed`,"DATA_ROUTER_HOSTILE_RESULT_FAILED");const record={fixture_id:f.fixture_id,fixture_class:f.class,fixture_file_sha256:info.file_sha256,entrypoint:f.vector.entrypoint,entrypoint_invoked:true,semantic_execution_completed:true,expected_outcome:expected.disposition,actual_outcome:actual.disposition,expected_route:expected.route,actual_route:actual.route,expected_error_code:expected.error_code,actual_error_code:actual.error_code,assertion_readbacks:checks,external_side_effects:actual.external_side_effects,duration_ms:0,result_sha256:null};record.result_sha256=resultDigest(record);results.push(record);}
const mutationSensitivity=await mutation(readJson(path.join(packageRoot,"fixtures/unsafe_action.json")));assert(mutationSensitivity.status==="WEAKENED"&&mutationSensitivity.mutation_detected,"Data Router mutation proof did not execute","DATA_ROUTER_MUTATION_PROOF_MISSING");const evaluation={schema:DATA_ROUTER_PACKAGE_EVALUATION_SCHEMA,version:1,status:"PASS",block_id:BLOCK_ID,lifecycle:"CANDIDATE",activation:"OFF",package_root_sha256:canonicalDigest(digests),package_block_sha256:block.block_sha256,gate_inventory_sha256:canonicalDigest(digests.filter(e=>e.relative_path.includes("/gates/"))),fixture_inventory_sha256:canonicalDigest(digests.filter(e=>e.relative_path.includes("/fixtures/"))),fixture_results:results,mutation_sensitivity:mutationSensitivity,independent_signature_required:true,observed_at_utc:new Date().toISOString(),evaluation_sha256:null};evaluation.evaluation_sha256=canonicalDigest({...evaluation,evaluation_sha256:null});return Object.freeze(evaluation);}
if(import.meta.url===`file://${process.argv[1]}`)process.stdout.write(`${JSON.stringify(await evaluateDataRouterPackage(),null,2)}\n`);
