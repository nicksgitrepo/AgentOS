#!/usr/bin/env node

/* Read-only Assurance/Enterprise router boundary. It classifies a typed
 * assurance signal and routes the smallest downstream context. It never
 * performs atomic assurance work or accepts a result. */

import {canonicalDigest} from "./content-addressing.mjs";
import {scanPersistedRecord} from "./persisted-record-privacy.mjs";

export const ASSURANCE_ENTERPRISE_ROUTER_BOUNDARY_SCHEMA = "agentos.assurance_enterprise_router_boundary_input.v1";
export const ASSURANCE_ENTERPRISE_ROUTER_RESULT_SCHEMA = "agentos.assurance_enterprise_router_boundary_result.v1";
const SAFE_ID = /^[A-Z][A-Z0-9._:-]{1,160}$/u;
const OPAQUE_REF = /^(?:opaque|ref):[A-Z0-9._:/-]{1,180}$/u;
const SHA256 = /^[0-9a-f]{64}$/u;
const REQUESTS = new Set(["CLASSIFY_ASSURANCE_SIGNAL", "ASSEMBLE_ATOMIC_CONTEXT", "ROUTE_ASSURANCE_HANDOFF", "NOT_APPLICABLE", "UNRELATED_REQUEST", "MERGE", "PUSH", "DELETE", "DEPLOY", "PUBLISH", "ACTIVATE", "APPROVE", "ACCEPT", "SPAWN", "ADMIT", "ARCHIVE", "DESPAWN", "WRITE_PROJECT", "WRITE_MEMORY", "SELF_REVIEW", "PERFORM_ATOMIC_ASSURANCE"]);
const FORBIDDEN_REQUESTS = new Set(["MERGE", "PUSH", "DELETE", "DEPLOY", "PUBLISH", "ACTIVATE", "APPROVE", "ACCEPT", "SPAWN", "ADMIT", "ARCHIVE", "DESPAWN", "WRITE_PROJECT", "WRITE_MEMORY", "SELF_REVIEW", "PERFORM_ATOMIC_ASSURANCE"]);
const ACTIONS = new Set(["CLASSIFY", "ASSEMBLE", "ROUTE"]);
const ALLOWED_TOOLS = new Set(["READ_SIGNAL", "READ_SOURCE_LOCK", "READ_CONTEXT", "READ_ROUTER_CATALOG"]);
const SIGNALS = new Set(["QA.TEST_ARCHITECT", "ASSURANCE", "TEST_ARCHITECTURE", "ASSURANCE_SIGNAL", "ATOMIC_SPECIALIST_CANDIDATE"]);
const EVIDENCE_KEYS = new Set(["authority_status", "custody_status", "custody_owner", "custody_ref", "source_status", "source_identity", "source_version", "candidate_identity", "candidate_digest", "candidate_status", "assurance_signal", "signal_status", "atomic_scope", "atomic_candidates", "context_status", "requested_action", "signals", "context_complete", "handoff_ref", "model_policy_status", "model_task_class", "model_route_status", "required_block_identities", "requested_tools", "self_acceptance", "scope_expanded", "authority_conflict", "project_data_present", "secret_data_present", "unbound_receipt", "unreviewed_gate", "unknown_context", "authority_scope", "sibling_authorities"]);
function fail(message, code = "ASSURANCE_ENTERPRISE_INPUT_INVALID") { const error = new Error(message); error.code = code; throw error; }
function assert(value, message, code) { if (!value) fail(message, code); }
function exactKeys(value, allowed, name) { assert(value && typeof value === "object" && !Array.isArray(value), `${name} must be an object`, "ASSURANCE_ENTERPRISE_SHAPE_INVALID"); for (const key of Object.keys(value)) assert(allowed.has(key), `${name} contains unknown field ${key}`, "ASSURANCE_ENTERPRISE_UNKNOWN_FIELD"); }
function bounded(value, name, max = 240) { assert(typeof value === "string" && value.length > 0 && value.length <= max, `${name} must be bounded`, "ASSURANCE_ENTERPRISE_FIELD_INVALID"); }
function safeId(value, name) { bounded(value, name); assert(SAFE_ID.test(value), `${name} is not canonical`, "ASSURANCE_ENTERPRISE_ID_INVALID"); }
function opaque(value, name) { bounded(value, name, 180); assert(OPAQUE_REF.test(value), `${name} is not opaque`, "ASSURANCE_ENTERPRISE_REF_INVALID"); }
function digest(value, name) { bounded(value, name, 64); assert(SHA256.test(value), `${name} is not SHA-256`, "ASSURANCE_ENTERPRISE_DIGEST_INVALID"); assert(!/^([0-9a-f])\1{63}$/u.test(value), `${name} is placeholder-like`, "ASSURANCE_ENTERPRISE_DIGEST_PLACEHOLDER"); }
function result(disposition, route, errorCode, input, extra = {}) { const base = {schema: ASSURANCE_ENTERPRISE_ROUTER_RESULT_SCHEMA, version: 1, disposition, route, error_code: errorCode, routing_allowed: false, acceptance_allowed: false, external_side_effects: {signal_reads: 0, context_writes: 0, atomic_work_calls: 0, acceptance_calls: 0, credential_accesses: 0, state_changes: 0}, input_sha256: canonicalDigest(input), ...extra}; return Object.freeze({...base, result_sha256: canonicalDigest({...base, result_sha256: null})}); }
function validateInput(input) { exactKeys(input, new Set(["schema", "version", "request_kind", "evidence"]), "assurance router input"); assert(input.schema === ASSURANCE_ENTERPRISE_ROUTER_BOUNDARY_SCHEMA && input.version === 1, "assurance router schema mismatch", "ASSURANCE_ENTERPRISE_SCHEMA_MISMATCH"); assert(typeof input.request_kind === "string" && REQUESTS.has(input.request_kind), "assurance router request kind is not recognized", "ASSURANCE_ENTERPRISE_REQUEST_INVALID"); exactKeys(input.evidence, EVIDENCE_KEYS, "assurance router evidence"); const e = input.evidence; for (const key of ["authority_status", "custody_status", "source_status", "source_identity", "source_version", "candidate_status", "assurance_signal", "signal_status", "atomic_scope", "context_status", "requested_action", "model_policy_status", "model_task_class", "model_route_status", "authority_scope"]) if (e[key] !== undefined) bounded(e[key], `evidence.${key}`); for (const key of ["custody_owner", "candidate_identity"]) if (e[key] !== undefined) safeId(e[key], `evidence.${key}`); for (const key of ["custody_ref", "handoff_ref"]) if (e[key] !== undefined) opaque(e[key], `evidence.${key}`); if (e.candidate_digest !== undefined) digest(e.candidate_digest, "evidence.candidate_digest"); for (const key of ["signals", "atomic_candidates", "required_block_identities", "requested_tools", "sibling_authorities"]) if (e[key] !== undefined) { assert(Array.isArray(e[key]) && e[key].length <= 8, `${key} must be bounded`, "ASSURANCE_ENTERPRISE_LIST_INVALID"); for (const value of e[key]) safeId(value, key); } for (const key of ["context_complete", "self_acceptance", "scope_expanded", "authority_conflict", "project_data_present", "secret_data_present", "unbound_receipt", "unreviewed_gate", "unknown_context"]) if (e[key] !== undefined) assert(typeof e[key] === "boolean", `${key} must be boolean`, "ASSURANCE_ENTERPRISE_BOOLEAN_INVALID"); assert(scanPersistedRecord(input).safe, "assurance router evidence contains protected or secret-like data", "ASSURANCE_ENTERPRISE_PRIVACY_DENIED"); }
function missing(e) { return ["authority_status", "custody_status", "custody_owner", "custody_ref", "source_status", "source_identity", "source_version", "candidate_identity", "candidate_digest", "candidate_status", "assurance_signal", "signal_status", "atomic_scope", "atomic_candidates", "context_status", "requested_action", "signals", "context_complete", "handoff_ref", "model_policy_status", "model_task_class", "model_route_status", "required_block_identities"].filter((key) => e[key] === undefined || e[key] === null || e[key] === "" || (Array.isArray(e[key]) && e[key].length === 0)); }
export function evaluateAssuranceEnterpriseRouterBoundary(input) { validateInput(input); const e = input.evidence; if (input.request_kind === "NOT_APPLICABLE" || input.request_kind === "UNRELATED_REQUEST") return result("DENY", "NO_ASSURANCE_SCOPE", "ASSURANCE_ENTERPRISE_SCOPE_NOT_APPLICABLE", input); if (FORBIDDEN_REQUESTS.has(input.request_kind)) return result("DENY", "NO_ROUTER_SIDE_EFFECT", "ASSURANCE_ENTERPRISE_OPERATION_FORBIDDEN", input); if (e.self_acceptance === true) return result("DENY", "INDEPENDENT_REVIEW_REQUIRED", "ASSURANCE_ENTERPRISE_SELF_ACCEPTANCE_FORBIDDEN", input); if (e.authority_conflict === true) return result("DENY", "CONTROLLER_ESCALATION", "ASSURANCE_ENTERPRISE_AUTHORITY_CONFLICT", input); if (e.scope_expanded === true) return result("DENY", "NARROW_SCOPE_REQUIRED", "ASSURANCE_ENTERPRISE_SCOPE_EXPANSION_FORBIDDEN", input); if (e.project_data_present === true || e.secret_data_present === true) return result("DENY", "PRIVACY_BOUNDARY_REQUIRED", "ASSURANCE_ENTERPRISE_PROTECTED_DATA_FORBIDDEN", input); if (e.unbound_receipt === true || e.unreviewed_gate === true || e.unknown_context === true) return result("DENY", "TYPED_CONTEXT_REQUIRED", "ASSURANCE_ENTERPRISE_EVIDENCE_UNBOUND", input); if (e.authority_scope === "UMBRELLA") return result("DENY", "NARROW_SCOPE_REQUIRED", "ASSURANCE_ENTERPRISE_UMBRELLA_AUTHORITY_FORBIDDEN", input); if (e.sibling_authorities?.length > 1 || e.sibling_authorities?.includes("DUPLICATE")) return result("DENY", "SINGLE_AUTHORITY_REQUIRED", "ASSURANCE_ENTERPRISE_DUPLICATE_AUTHORITY", input); const absent = missing(e); if (absent.length) return result("DENY", "TYPED_CONTEXT_REQUIRED", "ASSURANCE_ENTERPRISE_CONTEXT_INCOMPLETE", input, {missing_fields: absent}); if (e.authority_status !== "CURRENT" || e.custody_status !== "BOUND" || e.custody_owner !== "AGENT.ASSURANCE_ENTERPRISE_ROUTER") return result("DENY", "AUTHORITY_REFRESH_REQUIRED", "ASSURANCE_ENTERPRISE_AUTHORITY_UNVERIFIED", input); if (e.source_status !== "CURRENT" || e.source_version !== "1" || e.source_identity !== "SOURCE.ATOMIC_SPECIALIZATION_LAW") return result("DENY", "SOURCE_REFRESH_REQUIRED", "ASSURANCE_ENTERPRISE_SOURCE_STALE_OR_UNVERIFIED", input); if (e.candidate_status !== "CURRENT_CANDIDATE" || e.signal_status !== "BOUND" || e.context_status !== "ROUTER_CONTEXT") return result("DENY", "CANDIDATE_CONTEXT_REQUIRED", "ASSURANCE_ENTERPRISE_CONTEXT_BINDING_INVALID", input); if (e.context_complete !== true) return result("DENY", "TYPED_CONTEXT_REQUIRED", "ASSURANCE_ENTERPRISE_CONTEXT_INCOMPLETE", input); if (!ACTIONS.has(e.requested_action)) return result("DENY", "TYPED_CONTEXT_REQUIRED", "ASSURANCE_ENTERPRISE_ACTION_INVALID", input); if (!Array.isArray(e.requested_tools) || e.requested_tools.length === 0 || e.requested_tools.some((tool) => !ALLOWED_TOOLS.has(tool))) return result("DENY", "READ_ONLY_TOOLS_REQUIRED", "ASSURANCE_ENTERPRISE_TOOL_SCOPE_FORBIDDEN", input); if (!Array.isArray(e.signals) || e.signals.length === 0 || e.signals.some((signal) => !SIGNALS.has(signal))) return result("DENY", "TYPED_CONTEXT_REQUIRED", "ASSURANCE_ENTERPRISE_SIGNAL_UNSUPPORTED", input); if (e.assurance_signal !== "QA.TEST_ARCHITECT" || e.atomic_scope !== "NARROW_ASSURANCE_CLASSIFICATION" || !Array.isArray(e.atomic_candidates) || e.atomic_candidates.length < 1 || e.atomic_candidates.some((id) => id === "UMBRELLA")) return result("DENY", "ATOMIC_SPECIALIST_REQUIRED", "ASSURANCE_ENTERPRISE_ATOMIC_ROUTE_INVALID", input); if (!Array.isArray(e.required_block_identities) || e.required_block_identities.length !== 2 || new Set(e.required_block_identities).size !== 2 || !e.required_block_identities.includes("SPECIALIST.FOUNDATION.ROLE_INTAKE_CLASSIFIER") || !e.required_block_identities.includes("SPECIALIST.FOUNDATION.SCOPE_NON_GOAL_GATE")) return result("DENY", "SOURCE_REFRESH_REQUIRED", "ASSURANCE_ENTERPRISE_BLOCK_BINDING_INVALID", input); if (e.model_policy_status !== "CURRENT" || e.model_route_status !== "BOUND" || e.model_task_class !== "DETERMINISTIC_QA") return result("DENY", "MODEL_POLICY_REFRESH_REQUIRED", "ASSURANCE_ENTERPRISE_MODEL_ROUTE_INVALID", input); return result("ROUTE", "ASSURANCE_ATOMIC_HANDOFF", "ASSURANCE_ENTERPRISE_ROUTE_READY", input, {routing_allowed: true, selected_owner: "AGENTOS.ORCHESTRATOR", handoff: {status: "WAITING_WITH_RECEIPT", next_action: "Route the typed assurance signal to the smallest applicable atomic specialist; do not perform assurance work or accept results.", execution_instruction: false}}); }
