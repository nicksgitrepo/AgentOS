# Audit Results

Status: Waiting for six independent audit reports.

The Orchestrator replaces this summary with the ordered issue list after all six handoffs are accepted. Each item must link to one file in `issues/` and use the standard state names defined by the collaborative-audit contract.
