{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agentos.spawner_bootstrap_preparation.v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["canonical_global_memory_genesis", "external_clearance_store", "external_review_store", "production_signing_key_embedded", "admission_before_preparation"],
  "properties": {
    "canonical_global_memory_genesis": {"const": "SEALED_REPOSITORY_AUTHORITY"},
    "external_clearance_store": {"const": "FIXED_SEPARATE_EVALUATOR_HANDOFF"},
    "external_review_store": {"const": "FIXED_SEPARATE_EVALUATOR_HANDOFF"},
    "production_signing_key_embedded": {"const": false},
    "admission_before_preparation": {"const": "FAIL_CLOSED"}
  }
}
