# Agent Builder typed test adapter

The copied schemas, interfaces, validators, and fixtures are a candidate-only
input. They compile task/context contracts for deterministic local tests but
grant no authority and do not admit or activate a role. The source package is
bound by the sanitized receipt and relative file manifest only.
